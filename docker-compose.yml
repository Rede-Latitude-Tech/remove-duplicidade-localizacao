version: "3.8"

services:
  # Redis para BullMQ (filas de jobs) e cache
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
